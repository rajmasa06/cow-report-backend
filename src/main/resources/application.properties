# =================================================================
# 1. Application Basic Info
# -----------------------------------------------------------------
spring.application.name=cow-report-backend

server.port=8080
server.address=0.0.0.0

# =================================================================
# 2. Mail Configuration (Gmail SMTP)
# -----------------------------------------------------------------

# =================================================================
# 2. Mail Configuration (Gmail SMTP)
# -----------------------------------------------------------------
spring.mail.host=smtp.gmail.com
spring.mail.port=465  <-- PORT CHANGED TO 465
spring.mail.username=mraj82931@gmail.com
spring.mail.password=xydzadiwbgoskqfw
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=false  <-- CHANGED TO FALSE
spring.mail.properties.mail.smtp.ssl.enable=true       <-- NEW LINE ADDED
spring.mail.properties.mail.debug=true
#app.server.base-url=http://localhost:8080
app.server.base-url	https://cow-report-backend-18.onrender.com/
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB


# =================================================================
# 3. MySQL Database Configuration (Corrected)
# -----------------------
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
server.tomcat.connection-timeout=120000
  
  


















